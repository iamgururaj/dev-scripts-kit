#!/bin/bash
source "$(dirname "$0")/log_info.sh"
# nid.sh - npm install --save-dev (accepts package names)

if [ $# -eq 0 ]; then
  echo "Usage: nid.sh <package-names>"
  exit 1
fi
log_run npm install --save-dev "$@"
